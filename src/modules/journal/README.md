Журнал предоставляет синхронные API для записей логов. Логи накапливаются в 
переменных журнала и по таймеру сбрасываются в БД (Mongo).

В зависимости от типа события, лог может пойти как в БД, так и в файл.
